<div class="flex-1 w-full max-w-md mx-auto flex items-center justify-center">
	<div class="w-full rounded-md shadow-md p-6 bg-white">
		<div class="flex flex-col gap-4">
			<h1 class="text-3xl text-center"><%= locals.author.name %></h1>

			<div class="flex flex-col mt-8 gap-5 flex-1">
				<div class="flex items-center">
					<span
						class="uppercase font-light text-sm text-zinc-600 basis-[128px] shrink-0"
						>Gender:
					</span>
					<span><%= locals.author.gender %></span>
				</div>

				<div class="flex items-center">
					<span
						class="uppercase font-light text-sm text-zinc-600 basis-[128px] shrink-0">
						Nationality:
					</span>
					<span><%= locals.author.nationality %></span>
				</div>

				<div class="flex items-center">
					<span
						class="uppercase font-light text-sm text-zinc-600 basis-[128px] shrink-0"
						>Date of Birth:
					</span>
					<span><%= locals.author.dob %></span>
				</div>

				<div class="flex flex-col gap-2">
					<span class="uppercase font-light text-sm text-zinc-600"
						>Biography:
					</span>
					<span><%= locals.author.bio %></span>
				</div>

				<div class="flex">
					<a
						href="/authors/<%= author.id %>/books"
						class="text-blue-500 font-light underline">
						See Books...
					</a>
				</div>

				<div class="flex justify-between gap-5 mt-8">
					<a
						href="/authors/<%= author.id %>/confirm-deletion?from=<%= encodeURIComponent(currentPath) %>"
						class="text-red-500 text-center w-24 py-0.5 rounded-sm border border-red-500 flex items-center justify-center hover:bg-red-500 hover:text-zinc-50 transition-colors duration-200">
						Delete
					</a>

					<a
						href="/authors/<%= author.id %>/edit?from=<%= encodeURIComponent(currentPath) %>"
						class="text-blue-500 text-center w-24 py-0.5 rounded-sm border border-blue-500 flex items-center justify-center hover:bg-blue-500 hover:text-zinc-50 transition-colors duration-200">
						Edit
					</a>
				</div>
			</div>
		</div>
	</div>
</div>
